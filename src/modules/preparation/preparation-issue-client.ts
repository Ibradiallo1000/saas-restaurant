import type { User } from "firebase/auth"
export async function executePreparationIssue(input:{user:User;restaurantId:string;type:"REPORT"|"RESOLVE";orderId:string;orderItemId:string;reason?:string;comment?:string}){const token=await input.user.getIdToken(),response=await fetch(`/api/restaurants/${encodeURIComponent(input.restaurantId)}/preparation-issues/commands`,{method:"POST",headers:{authorization:`Bearer ${token}`,"content-type":"application/json"},body:JSON.stringify({type:input.type,orderId:input.orderId,orderItemId:input.orderItemId,reason:input.reason,comment:input.comment||""})});const data=await response.json().catch(()=>null);if(!response.ok||!data?.ok)throw new Error(data?.error||"Signalement impossible.");return data}
