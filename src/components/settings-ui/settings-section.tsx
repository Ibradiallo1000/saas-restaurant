import * as React from "react"
import { DashboardWidget, DashboardWidgetFooter } from "@/components/dashboard-ui"
import { cn } from "@/lib/utils"
import type { SettingsSectionState } from "./settings-foundations"

export interface SettingsSectionProps extends Omit<React.HTMLAttributes<HTMLElement>, "title"> { id: string; title: React.ReactNode; description?: React.ReactNode; icon?: React.ReactNode; status?: React.ReactNode; state?: SettingsSectionState; actions?: React.ReactNode; footer?: React.ReactNode; headingAs?: "h2" | "h3" }
export const SettingsSection = React.forwardRef<HTMLElement, SettingsSectionProps>(({ actions, children, className, description, footer, headingAs: Heading = "h2", icon, id, state = "ready", status, title, ...props }, ref) => <DashboardWidget ref={ref} as="section" id={id} aria-labelledby={`${id}-title`} aria-busy={state === "loading" || undefined} className={cn("bg-[var(--settings-panel)]", className)} {...props}><header className="flex flex-wrap items-start justify-between gap-3 border-b border-[var(--settings-divider)] px-4 py-4 sm:px-5"><div className="flex min-w-0 gap-3">{icon ? <span aria-hidden="true" className="mt-0.5 shrink-0 text-[var(--data-info)] [&_svg]:size-5">{icon}</span> : null}<div className="min-w-0"><Heading id={`${id}-title`} className="text-[length:var(--text-dashboard-section-title)] font-semibold leading-[var(--leading-dashboard-section-title)]">{title}</Heading>{description ? <p className="mt-1 max-w-3xl text-sm leading-5 text-[var(--settings-muted)]">{description}</p> : null}</div></div><div className="flex flex-wrap items-center gap-2">{status}{actions}</div></header><div className="p-4 sm:p-5">{children}</div>{footer ? <DashboardWidgetFooter>{footer}</DashboardWidgetFooter> : null}</DashboardWidget>)
SettingsSection.displayName = "SettingsSection"
